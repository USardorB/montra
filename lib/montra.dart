import 'package:flutter/material.dart';

class Montra extends StatelessWidget {
  const Montra({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
  debugShowCheckedModeBanner: false,

      home: const MyHomePage(),
    );
  }
}
